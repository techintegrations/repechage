{%- style -%}
.custom-gallery--{{ section.id }} {
    display: grid;
    gap: 10px;
}

.custom-gallery--{{ section.id }} .gallery-item {
    break-inside: avoid;
    margin-bottom: 10px;
}

.custom-gallery--{{ section.id }}.column-1 {
    grid-template-columns: repeat(1, 1fr);
}

.custom-gallery--{{ section.id }}.column-2 {
    grid-template-columns: repeat(2, 1fr);
}

.custom-gallery--{{ section.id }}.column-3 {
    grid-template-columns: repeat(3, 1fr);
}

.custom-gallery--{{ section.id }}.column-4 {
    grid-template-columns: repeat(4, 1fr);
}

.custom-gallery--{{ section.id }}.column-5 {
    grid-template-columns: repeat(5, 1fr);
}

.custom-gallery--{{ section.id }}.column-6 {
    grid-template-columns: repeat(6, 1fr);
}

.custom-gallery--{{ section.id }}.column-7 {
    grid-template-columns: repeat(7, 1fr);
}

.custom-gallery--{{ section.id }}.column-8 {
    grid-template-columns: repeat(8, 1fr);
}

.custom-gallery--{{ section.id }} .gallery-item img {
    width: 100%;
    height: auto;
    display: block;
}
{%- endstyle -%}

<div class="page-width" data-aos="logo__animation">
  {%- if section.settings.title != blank -%}
    <div class="section-header">
      <h2 class="section-header__title">{{ section.settings.title | escape }}</h2>
    </div>
  {%- endif -%}

  {%- if section.blocks.size > 0 -%}
    <div class="custom-gallery column-{{ section.settings.image_per_row }} custom-gallery--{{ section.id }}">
      {% for block in section.blocks %}
        {% case block.type %}
          {% when 'image' %}
            {% if block.settings.image != blank %}
              <div  class="gallery-item image-item">
              <a href="{{ block.settings.image | image_url : width: 1200 }}" data-fancybox="gallery">
                <img src="{{ block.settings.image | image_url : width: 450 }}" width="" height="">
              </a>
              </div>
            {% endif %}
          {% when 'video' %}
          <div  class="gallery-item video-item">
            <a href="{{ block.settings.self_hosted_url }}" data-fancybox="gallery">
               <img src="{{ block.settings.video_thumbnail | image_url : width: 450 }}" width="" height="">
            </a>
          </div>
        {% endcase %}
      {% endfor %}
    </div>
  {%- endif -%}
</div>

{%- if section.settings.divider -%}</div>{%- endif -%}

{% schema %}
{
  "name": "Gallery",
  "class": "index-section custom-gallery-wrap",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:sections.logo-list.settings.title.label"
    },
    {
      "type": "range",
      "id": "image_per_row",
      "label": "Images Per row",
      "default": 4,
      "min": 2,
      "max": 8,
      "step": 1
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "image_caption",
          "label": "Image Caption"
        }
      ]
    },
    {
      "type": "video",
      "name": "Video",
      "settings": [
        {
          "type": "url",
          "id": "self_hosted_url",
          "label": "Self Hosted video url"
        },
         {
          "type": "image_picker",
          "id": "video_thumbnail",
          "label": "Video Thumbnail"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Gallery"
    }
  ],
  "disabled_on": {
    "groups": ["footer", "header", "custom.popups"]
  }
}
{% endschema %}
